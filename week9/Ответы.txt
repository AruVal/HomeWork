1. Какие бывают варианты объявления переменных?

для объявления переменных в JavaScript используют 3 оператора:
- var; Этот способ используется для объявления переменных, которые можно впоследствии изменить.
- let; Этот способ используется для объявления переменных, которые можно впоследствии изменить.

var и let похожи, но различаются. 
1) Переменная, которая объявлена при помощи «let», видна только в пределах кода, обозначенного «фигурными скобками».
А переменная, объявленная через «var», видна во всей функции даже за пределами кода, обозначенного «{...}», 
2) Переменная, которая объявлена с использованием оператора «let», становится доступной после ее объявления.
Переменная, которая объявлена с использованием оператора «var», будет доступной еще до ее объявления. 
То есть можно к ней обратиться и провести какие-то операции, а только потом объявить ее значение.
Объявление переменных через «let» помогает в коддинге, так как ограничивает проведение операций с неизвестными переменными.
3) Переменная, которая объявлена при помощи «var», объявляется один раз и используется во всех итерациях цикла, 
а иногда даже и после цикла. С переменной, объявленной при помощи «let», все совсем не так. Для каждой отдельной итерации
цикла необходимо объявлять собственную независимую переменную «let».

- const. Этот способ используется для объявления переменных с постоянным и впоследствии неизменным значением.

2. console.log('Hello World');

3. <script>
        let name = "Ilya";
        alert( 'hello ${1}' ); //hello 1
        alert( 'hello ${"name"}' ); //hello name
        alert( 'hello ${name}' ); //hello Ilya
    </script>

4. z = 7;

5. Какими тремя способами можно подключить JavaScript код?

1) Встроить скрипт в страницу HTML через тег <script>;
2) Записать скрипт на JavaScript в отдельный файл и потом подключить через <script src = "script.js"></script>. 
Атрибут type означает, какой язык вы подключаете, а атрибут src указывает на подключаемый файл. 
Файлы содержащие только код на JavaScript, имеют расширение .js.
3) Нашла такой способ - поместить код непосредственно в атрибут события HTML-элемента.
<head>

<script>
    function myFunction() {
    document.getElementById("demo").innerHTML = "Привет, javascript!";
    }
</script>

</head>
<body>

<p id="demo">Привет, мир!</p>
<button type="button" onclick="myFunction()">Кликни меня</button>

</body>

Это то, что нужно? Или я забежала вперед?

6. Есть ли какая-то разница между записями typeof str и typeof(str)?
Оператор typeof возвращает тип аргумента.
У него есть два синтаксиса: со скобками и без:
Синтаксис оператора: typeof x.
Синтаксис функции: typeof(x).
Работают они одинаково, но первый синтаксис короче. Таким образом, разницы нет.

7. 28

8. Результаты вычислений:
"" + 1 + 0 = "10"
"" - 1 + 0 = -1
1 (интересный пример, выполнила его через console.log. 
let v = true+false;
console.log(v);
)
2
6
"9px"
"$45"
2
NaN
Infinity
-9 5
-14
1
NaN
-2

9. х=2;

10. х=3;











